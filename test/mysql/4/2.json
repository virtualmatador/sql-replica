{
  "name": "demo",
  "tables": null,
  "routines": [
    {
      "type": "FUNCTION",
      "name": "double_value",
      "definition": "(`input_value` int) RETURNS int DETERMINISTIC NO SQL SQL SECURITY INVOKER BEGIN RETURN input_value * 3; END"
    }
  ],
  "users": [
    {
      "name": "Runner",
      "permissions": [
        {"type": "function", "subject": "double_value", "operations": ["EXECUTE"]}
      ]
    }
  ]
}
